services:
  - type: web
    name: calendar-booking
    runtime: docker
    region: frankfurt
    plan: starter
    branch: main
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true
